version: 0.2

phases:
  pre_build:
    commands:

  build:
    commands:
      #- cd eks-terr-cicd-AEMbase
      - echo Build started on `date`
      - sudo apt install unzip
      - unzip -q aem-zip
      - cd aem-zip
      - ls -ltr
      - cd aem-base_image
      - echo Building the Docker image......   
      - docker build -t aem-base-image .
      #- docker tag aem-base-image:latest 077344799555.dkr.ecr.us-east-1.amazonaws.com/aem-base-image:latest      
      #- docker push 077344799555.dkr.ecr.us-east-1.amazonaws.com/aem-base-image:latest
      - docker images
      - cd ..
      - cd aem-author
      - docker build -t aem-author .
      - docker images
      #- docker run -p 4502:4502 aem-author
      #- curl http://localhost:4502
      - docker tag aem-author:latest 077344799555.dkr.ecr.us-east-1.amazonaws.com/aem-author:latest
  post_build:
     commands:
       - echo Build completed on `date`
       - echo Pushing the Docker image....
#      - cd ..
#      - cd aem-author
       - docker push 077344799555.dkr.ecr.us-east-1.amazonaws.com/aem-author:latest
        
